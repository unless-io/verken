  <% if params[:controller] == "pages" %>
  <div class="navbar-verken single-item">
  <% else %>
    <div class="navbar-verken multi-item">
      <%= link_to root_path, class: "logo logo-small" do %>
        VER
        <br>
        KEN
      <% end %>
  <% end %>
    <div class="nav-block">
      <% if user_signed_in? %>
          <%= link_to dashboard_path, class: "nav-block-link" do %>
            <i class="fa fa-user"></i>
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: "nav-block-link" do %>
            <i class="fa fa-chevron-right"></i>
          <% end %>
      <% end %>
      <% if user_signed_in? %>
        <div id="sub">
          <%= link_to "dashboard", dashboard_path  %>
          <div class="divider"></div>
          <%= link_to "log out", destroy_user_session_path, method: :delete %>
        </div>
        <div class="clearfix"></div>
      <% end %>
    </div>
</div>
